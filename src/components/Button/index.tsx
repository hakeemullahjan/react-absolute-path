import SearchBar from "@components/SearchBar";
import { FC } from "react";

const Button: FC = () => {
  return (
    <div>
      <SearchBar />
      <h1>Button</h1>
    </div>
  );
};

export default Button;
